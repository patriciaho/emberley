<% if current_user %>

  <% if current_user.username == 'emberley' %>
    
    <%= render partial: 'admin' %>

  <% else %>
    
    <!-- User accessible page -->
    <%= render partial: 'user' %>

  <% end %>

<% else %>

  <!-- Welcome Screen -->
  <div class="main"></div>
  <div class="intro">
  	<h1>ABOUT THIS SITE</h1>
  </div>

<% end %>

<!-- Sign Up Pop-up Screen-->
<%= render partial: 'layouts/signup' %>